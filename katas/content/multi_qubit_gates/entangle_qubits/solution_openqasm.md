The CNOT gate (`cx`) creates entanglement by flipping the target qubit when the control qubit is in state $\ket{1}$.

@[solution]({
    "id": "multi_qubit_gates__entangle_qubits_solution_openqasm",
    "codePath": "./Solution.qasm"
})
