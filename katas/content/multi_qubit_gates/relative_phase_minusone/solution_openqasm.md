The `cz` (controlled-Z) gate applies a phase of $-1$ to the $\ket{11}$ state while leaving all other basis states unchanged.

@[solution]({
    "id": "multi_qubit_gates__relative_phase_minusone_solution_openqasm",
    "codePath": "./Solution.qasm"
})
