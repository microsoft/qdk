Apply the `s` gate to the first qubit and the `y` gate to the third qubit. The second qubit is left unchanged (identity operation).

@[solution]({
    "id": "multi_qubit_gates__compound_gate_solution_openqasm",
    "codePath": "./Solution.qasm"
})
