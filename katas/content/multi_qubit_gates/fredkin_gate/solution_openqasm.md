The `cswap` (Fredkin) gate swaps the second and third qubits when the first qubit is in the $\ket{1}$ state.

@[solution]({
    "id": "multi_qubit_gates__fredkin_gate_solution_openqasm",
    "codePath": "./Solution.qasm"
})
