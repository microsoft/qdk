The Toffoli gate (`ccx`) is a doubly-controlled NOT gate. It flips the target qubit (third) only when both control qubits (first and second) are in state $\ket{1}$.

@[solution]({
    "id": "multi_qubit_gates__toffoli_gate_solution_openqasm",
    "codePath": "./Solution.qasm"
})
