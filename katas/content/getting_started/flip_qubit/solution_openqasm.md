For this exercise, you can solve the problem by simply using the `x` gate on the input qubit, which will perform a "bit flip" and set the qubit into the $\ket{1}$ state.

@[solution]({
    "id": "getting_started__flip_qubit_solution_openqasm",
    "codePath": "./Solution.qasm"
})
