Create a three-qubit entangled state by first entangling Bob and Charlie with `h` and `cx`, then entangling Alice with Charlie using another `h` and `cx` pair.

@[solution]({
    "id": "teleportation__entangled_trio_solution_openqasm",
    "codePath": "./Solution.qasm"
})
