To prepare the $\ket{-}$ state, first apply `x` to flip to $\ket{1}$, then apply the Hadamard gate `h` to get $\frac{1}{\sqrt{2}}(\ket{0} - \ket{1})$.

@[solution]({
    "id": "preparing_states__minus_state_solution_openqasm",
    "codePath": "./Solution.qasm"
})
