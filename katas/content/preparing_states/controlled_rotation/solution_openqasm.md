Apply `h` to the first qubit to create a superposition, then apply `ch` (controlled-Hadamard) to apply H to the second qubit conditioned on the first qubit being $\ket{1}$.

@[solution]({
    "id": "preparing_states__controlled_rotation_solution_openqasm",
    "codePath": "./Solution.qasm"
})
