Apply `h` then `z` to the first qubit to prepare the $\ket{-}$ state, and `h` then `s` to the second qubit to prepare the $\ket{i}$ state. The tensor product gives an equal superposition with the required complex phases.

@[solution]({
    "id": "preparing_states__even_sup_two_qubits_complex_phases_solution_openqasm",
    "codePath": "./Solution.qasm"
})
