Apply `h` to both qubits to create an equal superposition, then apply `cz` to introduce a phase of $-1$ on the $\ket{11}$ component.

@[solution]({
    "id": "preparing_states__even_sup_two_qubits_phase_flip_solution_openqasm",
    "codePath": "./Solution.qasm"
})
