Prepare the state $\ket{10\dots0}$ by applying `x` to the first qubit, then apply the QFT. This produces the periodic state with frequency $F = 2^{n-1}$, which has alternating $+1$ and $-1$ amplitudes.

@[solution]({
    "id": "qft__alternating_amplitudes_solution_openqasm",
    "codePath": "./Solution.qasm"
})
