Measuring a qubit in the computational basis returns `0` for $\ket{0}$ and `1` for $\ket{1}$. Converting the measurement result to a boolean gives `true` when the qubit is $\ket{1}$.

@[solution]({
    "id": "distinguishing_states__zero_one_solution_openqasm",
    "codePath": "./Solution.qasm"
})
