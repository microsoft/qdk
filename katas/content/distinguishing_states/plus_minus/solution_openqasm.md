Applying the Hadamard gate maps $\ket{+}$ to $\ket{0}$ and $\ket{-}$ to $\ket{1}$. Measuring after the Hadamard returns `0` for $\ket{+}$ and `1` for $\ket{-}$. Since we want to return `true` for $\ket{+}$, we negate the boolean result.

@[solution]({
    "id": "distinguishing_states__plus_minus_solution_openqasm",
    "codePath": "./Solution.qasm"
})
