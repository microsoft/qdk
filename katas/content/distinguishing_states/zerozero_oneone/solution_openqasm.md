Since $\ket{00}$ and $\ket{11}$ differ in the value of each qubit, measuring any single qubit is enough. Measuring the first qubit gives `0` for $\ket{00}$ and `1` for $\ket{11}$, which directly gives the answer.

@[solution]({
    "id": "distinguishing_states__zerozero_oneone_solution_openqasm",
    "codePath": "./Solution.qasm"
})
