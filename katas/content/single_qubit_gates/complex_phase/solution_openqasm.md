The `p` (phase) gate applies a phase shift of $e^{i\alpha}$ to the $\ket{1}$ state while leaving $\ket{0}$ unchanged. This is equivalent to Q#'s `R1` gate.

@[solution]({
    "id": "single_qubit_gates__complex_phase_solution_openqasm",
    "codePath": "./Solution.qasm"
})
