The `s` gate applies a $\frac{\pi}{2}$ phase and the `t` gate applies a $\frac{\pi}{4}$ phase to the $\ket{1}$ state, combining to a total phase of $\frac{3\pi}{4}$.

@[solution]({
    "id": "single_qubit_gates__three_quarters_pi_phase_solution_openqasm",
    "codePath": "./Solution.qasm"
})
