The Z gate (Pauli-Z) applies a phase flip, leaving $\ket{0}$ unchanged and mapping $\ket{1}$ to $-\ket{1}$. In OpenQASM, this is the `z` gate.

@[solution]({
    "id": "single_qubit_gates__sign_flip_solution_openqasm",
    "codePath": "./Solution.qasm"
})
