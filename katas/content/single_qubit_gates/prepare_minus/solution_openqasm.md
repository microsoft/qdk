To prepare the $\ket{-}$ state from $\ket{0}$, first apply the `x` gate to get $\ket{1}$, then apply the Hadamard gate `h` to get $\frac{1}{\sqrt{2}}(\ket{0} - \ket{1})$.

@[solution]({
    "id": "single_qubit_gates__prepare_minus_solution_openqasm",
    "codePath": "./Solution.qasm"
})
