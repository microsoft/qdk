To apply a sign flip on the $\ket{0}$ state, we conjugate the Z gate with X gates. The sequence `x`, `z`, `x` effectively applies $-1$ phase to $\ket{0}$ while leaving $\ket{1}$ unchanged.

@[solution]({
    "id": "single_qubit_gates__sign_flip_on_zero_solution_openqasm",
    "codePath": "./Solution.qasm"
})
