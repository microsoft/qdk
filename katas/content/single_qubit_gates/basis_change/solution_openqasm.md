The Hadamard gate (`h`) creates an equal superposition, transforming $\ket{0}$ to $\frac{1}{\sqrt{2}}(\ket{0} + \ket{1})$.

@[solution]({
    "id": "single_qubit_gates__basis_change_solution_openqasm",
    "codePath": "./Solution.qasm"
})
