Applying `z`, `y`, and `x` gates in sequence produces a global phase of $i$, since $XYZ = iI$.

@[solution]({
    "id": "single_qubit_gates__global_phase_i_solution_openqasm",
    "codePath": "./Solution.qasm"
})
