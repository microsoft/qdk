The X gate (also known as the Pauli-X or NOT gate) flips the qubit state, transforming $\ket{0}$ to $\ket{1}$ and vice versa. In OpenQASM, this is the `x` gate.

@[solution]({
    "id": "single_qubit_gates__state_flip_solution_openqasm",
    "codePath": "./Solution.qasm"
})
