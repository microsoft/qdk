Apply the `x` gate to the second qubit to flip it to $\ket{1}$, then apply the `h` gate to put it into the $\ket{-}$ superposition state.

@[solution]({
    "id": "multi_qubit_systems__prepare_superposition_solution_openqasm",
    "codePath": "./Solution.qasm"
})
