Apply the `x` gate to both qubits to flip them from $\ket{0}$ to $\ket{1}$, preparing the $\ket{11}$ basis state.

@[solution]({
    "id": "multi_qubit_systems__prepare_basis_state_solution_openqasm",
    "codePath": "./Solution.qasm"
})
