Apply `h` to both qubits to create superpositions, then apply `s` to the first qubit (adding a $\frac{\pi}{2}$ phase) and `t` to the second qubit (adding a $\frac{\pi}{4}$ phase) to introduce complex amplitudes.

@[solution]({
    "id": "multi_qubit_systems__prepare_with_complex_solution_openqasm",
    "codePath": "./Solution.qasm"
})
