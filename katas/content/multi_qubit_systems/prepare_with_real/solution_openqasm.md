Apply the `h` gate to the first qubit to create a superposition, then apply `x` and `h` to the second qubit to prepare it in the $\ket{-}$ state. The resulting tensor product gives the desired state with real amplitudes.

@[solution]({
    "id": "multi_qubit_systems__prepare_with_real_solution_openqasm",
    "codePath": "./Solution.qasm"
})
