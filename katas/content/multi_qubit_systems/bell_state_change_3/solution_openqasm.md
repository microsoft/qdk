Apply `x` followed by `z` to the first qubit. The `x` gate swaps the computational basis components, and the `z` gate introduces a relative phase, transforming $\ket{\Phi^+}$ into $\ket{\Psi^-}$.

@[solution]({
    "id": "multi_qubit_systems__bell_state_change_3_solution_openqasm",
    "codePath": "./Solution.qasm"
})
