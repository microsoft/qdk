{
  "scripts": {
    "dev": "npm run build -- --sourcemap=inline --watch",
    "build": "npx esbuild js/index.tsx js/bloch.css --minify --format=esm --bundle --outdir=src/qsharp_widgets/static --loader:.woff2=dataurl --loader:.woff=empty --loader:.ttf=empty"
  },
  "devDependencies": {}
}
